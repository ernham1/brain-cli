# 핸드오프: Brain 프로젝트 복구 현황

작성: 2026-08-16 | 배경: AgentForge 세션에서 D드라이브 전체 삭제 사고 발생, Brain 포함 76개 프로젝트 손실

## 사고 개요

2026-08-16, AgentForge 세션에서 "C:\Projects가 D:\Projects의 완전 중복 복사본"이라고 잘못 판단하고 `Remove-Item -Recurse -Force`로 C:\Projects를 삭제했다. 실제로는 C와 D가 같은 물리적 실체(볼륨 마운트, Device/Inode 동일)였고, 이 삭제로 D:\Projects 하위 76개 프로젝트가 영향을 받았다.

**절대 반복하지 말 것: 삭제 가능성 있는 명령은 사용자 승인 없이 실행 금지.**

## Brain은 다른 프로젝트보다 상태가 좋다 — 중요

Brain은 삭제가 도중에 멈춘 "부분 손상" 케이스다. AIOS 등 완전 삭제된 프로젝트와 다르다.

## 현재 상태 (2026-08-16 기준 확인됨)

### 1. 워킹 트리 실물 — `D:\Projects\Brain` (원본 위치, 그대로 살아있음)
- **파일 20,012개 생존.** `src/brain-cli`, `src/brain-server`, `src/clo-telegram` 등 전체 구조 온전.
- node_modules 제외 실제 소스코드(.ts/.js/.mjs) 386개 확인됨.
- **이 파일들은 Recuva로 복구한 게 아니라, 어제 삭제 명령이 도중에 멈춰서 원래부터 지워지지 않고 남아있던 것이다.** 다른 복구 소스보다 신뢰도가 가장 높다.
- Codex 세션 로그에서 확인한 마지막 실제 코드 수정(2026-07-20, `intent-links.test.js`, `multi-bot-routing.test.mjs`)의 파일 수정시각이 `2026-07-28 14:24`로, git 마지막 커밋 시각(`2026-07-28 19:04`)과 거의 일치 — **이 워킹 트리가 마지막 커밋 시점과 같거나 그 이후 최신 상태일 가능성이 높다.**
- **다만 `.git` 폴더 자체는 삭제되어 없다.** 커밋 이력은 이 경로에 없음.

### 2. git 이력 — `C:\recover\Projects\Brain` (Recuva 복구본)
- Recuva(Deep Scan + Restore folder structure)로 별도 복구한 사본. `.git` 폴더가 존재함.
- `git log`: 커밋 23개 확인됨 (`4bdb05c 결정 다이제스트에 무작위 사후 감사 샘플 도입 (C안)`이 최신, 커밋 시각 2026-07-28 19:04:15).
- **주의: `git fsck --full` 결과 2929개 object가 손상**(`inflate: data stream error`). `.git/index`도 손상되어 `git status`가 바로 안 됨 — `git read-tree HEAD` 등으로 index 재생성 시도 필요.
- `git ls-tree HEAD`(최상위 파일 목록)는 정상 조회되지만, 실제 파일 내용(blob)을 `git cat-file -p`로 열어보면 다수가 손상되어 있음(표본 200개 중 정상 7개, 나머지 손상 또는 트리 진행 중단).
- **결론: git 이력(커밋 메시지, 브랜치 구조)은 최상위 레벨에서만 확인 가능하고, 실제 커밋별 파일 diff는 대부분 복원 불가.**

### 3. Codex 세션 로그 — `C:\Users\ernham\.codex\sessions\`
- `cwd: C:\Projects\Brain`으로 필터링한 세션들에서 실제 파일 수정(`apply_patch`)이 있는 것은 2026-07-01, 07-08(2건), 07-20 총 4개 세션, 13개 patch.
- **07-28 이후(git 마지막 커밋 이후) 시점에 Brain 프로젝트 경로에 대한 명확한 apply_patch 기록은 못 찾음.** 이사님이 "7/28 이후 Codex로도 수정했다"고 기억하시는 부분은 정확한 파일/내용이 특정되지 않은 상태 — 아래 "확인 필요" 참조.
- 형식: `.jsonl`(JSON Lines). 실제 파일 수정은 `payload.name === "apply_patch"`인 `custom_tool_call` 이벤트에 diff로 기록됨(`*** Begin Patch\n*** Update File: ...`).
- **주의: guardian(리스크 판정) 서브에이전트 세션이 섞여있다.** `head -1 세션파일`에서 `"subagent":{"other":"guardian"`이면 실제 작업이 아니라 다른 세션을 사후 평가하는 메타 로그이므로 건너뛸 것.
- **주의: cwd가 다른 프로젝트(예: `D:\Projects\3D_Create`)인데 patch 내용에 "brain"이라는 단어가 들어간 파일이 있어 오탐 가능.** 반드시 `Projects\Brain\src\...` 같은 정확한 경로 매칭으로 필터링할 것 (`institution-building` 같은 무관한 프로젝트 하위 폴더가 "brain" 키워드로 오검색됨).

### 4. 원격 저장소 — 확인됨, 그러나 제한적
`C:/recover/Projects/Brain/.git/config`에서 `remote origin = https://github.com/ernham1/brain-cli.git` 확인됨.
**단, `gh repo view ernham1/brain-cli` 결과 diskUsage 772KB, 마지막 push 2026-03-03 — 이건 Brain 프로젝트 전체가 아니라 초기 단계의 작은 하위 저장소로 추정된다.** 지금 Brain 프로젝트 규모(D드라이브 워킹 트리 20,012개 파일)와 비교하면 턱없이 작다. 이 원격을 주 소스로 쓰지 말 것 — 위 1번(D:\Projects\Brain 워킹 트리)이 최우선 소스라는 결론은 그대로 유효하다.
필요하면 `gh repo clone ernham1/brain-cli`로 별도 확인은 가능하나, 큰 도움은 안 될 것으로 예상.

## 확인이 필요한 것 (이사님 기억 vs 로그 불일치)

이사님은 "2026-07-28 이후에도 Codex로 Brain을 수정했다"고 기억하시는데, 로그 검색으로는 그 시점 이후의 명확한 Brain 프로젝트 patch를 찾지 못했다. 가능성:
1. 실제로는 다른 cwd(하위 패키지를 직접 cwd로 잡은 세션)에서 작업해서 필터가 놓쳤을 수 있음 — 어떤 파일/기능을 고쳤는지 구체적으로 기억나면 그 키워드로 재검색 필요.
2. 로컬 Codex 세션이 아니라 원격/다른 인터페이스로 작업했을 수 있음.
3. 착각이었을 수 있음 — 위 3항목(4세션, 13 patch)이 실제 마지막일 가능성.

## 다음 세션 작업 순서 제안

1. **`D:\Projects\Brain`의 워킹 트리(20,012개 파일)를 최우선 신뢰 소스로 삼는다.** Recuva 복구본보다 이게 훨씬 온전하다.
2. `C:\recover\Projects\Brain`의 `.git`에서 `git read-tree HEAD` 또는 `git reset --hard HEAD` 등으로 index 복구를 시도하고, 성공하면 그 `.git` 폴더만 `D:\Projects\Brain`으로 복사해서 워킹 트리와 결합한다. **단, 이 작업은 되돌리기 어려우니 반드시 별도 복사본에서 시도하고, 원본 워킹 트리를 덮어쓰지 않도록 백업 후 진행할 것.**
3. `.git` 결합 후 `git status`로 워킹 트리와 마지막 커밋(4bdb05c) 사이 차이가 있는지 확인 — 있다면 그게 07-20~07-28 사이 미커밋 변경사항일 수 있다.
4. 이사님께 "7/28 이후 무엇을 고쳤는지" 구체적으로 재확인 후, Codex 세션 로그 재검색.
5. 원격 저장소(Gitea 등)가 있는지 확인 — 있으면 그쪽에서 커밋 이력 전체를 정상 clone 가능.

## 참고: 손상 검사 스크립트 (다른 프로젝트에도 적용 가능)

```javascript
const fs = require('fs');
const path = require('path');

function isCorrupt(filePath) {
  const stat = fs.statSync(filePath);
  if (stat.size === 0) return false;
  const buf = fs.readFileSync(filePath);
  const sampleSize = Math.min(1024, buf.length);
  for (let i = 0; i < sampleSize; i++) {
    const c = buf[i];
    if (c !== 0x00 && c !== 0x20 && c !== 0x0a && c !== 0x0d && c !== 0x09) {
      return false; // 정상 내용 있음
    }
  }
  return true; // 손상(빈 값으로 채워짐)
}
```

**주의: 이 스크립트로 "정상"이라고 나온 파일도 node_modules/coding-workspaces 안의 서드파티 파일일 수 있으니(AIOS 조사에서 확인된 함정), 반드시 경로를 확인해서 실제 프로젝트 소스인지 재확인할 것.**
