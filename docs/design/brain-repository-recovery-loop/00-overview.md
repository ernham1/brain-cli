# Brain 저장소 재해복구 루프 — 개요

> 원본: `HANDOFF-복구-20260816.md` + `rec_proj_brain_20260816_0001`
> 선행: 2026-08-16 독립 진단·검증
> 설계일: 2026-08-16 | 루프 태그: `#데이터복구`

## 사용자 Job

광웅 이사님이 7월 28일 이후 변경까지 포함한 Brain 최신 소스를 잃지 않고, 다시 Git으로 추적·검증·백업할 수 있는 복구 저장소를 확보한다.

## 가정 명세

### 확정된 가정

- `C:\Projects\Brain`과 `D:\Projects\Brain`은 같은 파일 ID를 가리키므로 독립 사본이 아니다.
- 현재 워킹 트리는 7월 28일 이후 변경 12개를 포함한 유일 최신 소스다.
- 현재 워킹 트리에는 `.git`이 없다.
- `C:\recover\Projects\Brain\.git`은 23개 커밋 메타데이터가 있으나 객체 2,929개와 index가 손상됐다.
- 공개 원격 저장소는 20개 커밋까지만 보유한다.

### 기술적으로 결정한 것 (확인 불필요)

- 현 위치 직접 수리가 아니라 `C:\Projects` 심볼릭 링크 밖의 실제 C: NTFS 볼륨 `C:\Brain-Recovery\20260816`에서 재구성한다.
- 원격 20개 커밋을 정상 기반선으로 삼고, 손상 복구본은 이력·메타데이터 증거로만 사용한다.
- 최신 워킹 트리는 해시 검증된 복사본으로 옮긴 뒤 새 복구 체크포인트 커밋을 만든다.
- 복원 불가능한 과거 diff는 추측 생성하지 않고 provenance 문서에 손실 범위로 남긴다.
- 테스트와 기능 검증은 임시 데이터·별도 포트로 수행한다.

### 아직 열린 사항

- 결정 저널 최소 부트스트랩과 시작 게이트 기록은 완료됐다(`dj-20260816-1731-brr1`).
- 운영 경로를 복구 저장소로 전환하는 시점과 방법은 이번 루프 범위 밖이다.
- Brain 운영 데이터의 무결성 이슈 4건 처리는 저장소 복구 뒤 별도 루프로 수행한다.

## 범위

### 포함

- 현재 워킹 트리의 별도 볼륨 증거 보존과 해시 검증
- 공개 원격 저장소의 정상 clone과 Git 무결성 확인
- 손상 `.git`에서 회수 가능한 커밋·브랜치·메시지·부모관계 추출
- 최신 소스의 새 저장소 재구성
- 의존성 재설치, 테스트 정정, 빌드·기능 스모크
- 복구 커밋·Git bundle·provenance 문서 생성

### 미포함

- 현재 `C:\Projects\Brain` 또는 `D:\Projects\Brain` 삭제·이동·reset·덮어쓰기
- PM2 운영 서비스의 재시작·경로 전환
- 운영 Brain DB·Raw 데이터 수정
- 원격 push, PR, 배포
- 복원 불가능한 과거 파일 내용의 추측 복원

## 복구 아키텍처

```text
살아 있는 워킹 트리 (읽기 전용 증거)
        ├─ 전체 스냅샷 + SHA-256 manifest ──> C:\Brain-Recovery\20260816\evidence
        └─ 소스 선별 복사 ─────────────────> C:\Brain-Recovery\20260816\repo
                                                    ▲
공개 원격 20 commits ── fresh clone + fsck ─────────┤
손상 .git 23 commits ── 메타데이터/회수 가능 객체 ─┘
                                                    │
                     테스트·격리 스모크·provenance·bundle
```

## 설계 결정과 트레이드오프

| 결정 | 근거 | 트레이드오프 |
|---|---|---|
| 실제 C: 별도 작업공간 | `C:\Projects`→`D:\Projects` 링크 밖이라 원본 D:와 물리 분리 | 시스템 볼륨 여유 공간을 사용 |
| 원격 clone 기반 재구성 | 정상 Git 객체 20개 확보 | 로컬 3개 커밋은 별도 회수 필요 |
| 최신 트리 단일 체크포인트 | 최신 기능 보존이 과거 diff보다 우선 | 일부 과거 변경의 세분화된 이력 손실 |
| production cutover 분리 | 복구 검증 실패가 운영에 영향 주지 않음 | 운영 정상화까지 다음 루프 필요 |

## PRISM 검토

| Layer | 판정 | 비고 |
|---|---|---|
| R | ✅ | 원본 불변, 복사·해시 단계마다 즉시 PASS/FAIL 피드백 |
| B | ✅ | 보존 → Git 재구성 → 테스트 → bundle로 닫힌 복구 행동 루프 |
| D | ✅ | 안전 우선순위와 중단 조건을 체크리스트에 고정 |
| S | ⚠ | GitHub·C: 여유 공간·npm registry 외부 전제는 착수 시 실측 필요 |
| G | ✅ | live tree write/reset/delete, 운영 재시작, push를 금지 |
| I | ✅ | manifest·provenance·test report·bundle을 산출물 정본으로 사용 |
| E | ✅ | 이사님 Job은 최신 소스를 잃지 않고 다시 추적 가능한 저장소 확보 |

## 성공 조건

- 원본과 증거 스냅샷의 파일 목록·SHA-256 비교가 일치한다.
- 복구 저장소의 `git fsck --full`이 통과한다.
- 7월 28일 이후 확인된 12개 소스가 복구 저장소에 존재하고 원본과 해시가 일치한다.
- `brain-cli`와 `clo-telegram`의 빌드·전체 테스트가 exit code 0이다.
- 임시 데이터·별도 포트에서 health → write → recall 핵심 흐름이 통과한다.
- 복구 커밋, provenance, Git bundle이 생성되고 다시 검증된다.

## 중단 조건

- 원본 경로에 쓰기가 발생하거나 C/D를 독립 사본으로 취급하는 명령이 발견됨.
- 스냅샷 해시 불일치가 해결되지 않음.
- 격리 대상 C:가 원본 D:와 같은 Volume UniqueId이거나 Healthy/OK가 아님.
- 시크릿이 manifest·로그·커밋에 포함될 위험이 확인됨.
- 동일 항목 3회 실패 후 접근 전환 1회도 실패함.